<% @result.items.each do |item| %>
	<div>
		<span><%= item.name %> </span> <span> <%= item.price %></span> <%= link_to "Add to grocery list", order_index_path(user_id: current_user), method: :post, remote: true %>
<% end %>