<h1>OrderIngredients#index</h1>
<p>Find me in app/views/order_ingredients/index.html.erb</p>

<div id="order_ingredients_list">
	<%= render 'order_ingredients_list', order: @order %>
	<%= form_for @order, method: :put do |f| %>
		<%= f.hidden_field :available, value: true %>
		<%= f.submit "complete" %>
	<% end %>
</div>
